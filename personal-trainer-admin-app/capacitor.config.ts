import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'personal-trainer-admin-app',
  webDir: 'www'
};

export default config;
